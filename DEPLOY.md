# 🚀 Деплой на VildanAi.ru

## Что нужно сделать на сервере

### 1. Подготовка сервера

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker (если не установлен)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt install docker-compose -y

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

### 2. Настройка DNS

В панели управления доменом (где купили VildanAi.ru) настройте DNS записи:

```
Тип: A
Имя: @
Значение: IP_ВАШЕГО_СЕРВЕРА
TTL: 3600

Тип: A  
Имя: www
Значение: IP_ВАШЕГО_СЕРВЕРА
TTL: 3600
```

Проверка DNS (подождите 5-10 минут после настройки):
```bash
dig vildanai.ru
dig www.vildanai.ru
```

### 3. Открытие портов

```bash
# Открыть порты 80 и 443
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 443/udp
sudo ufw reload

# Проверка
sudo ufw status
```

### 4. Клонирование проекта

```bash
# Перейдите в нужную директорию
cd /opt
sudo git clone <ваш-репозиторий> gremuiv
cd gremuiv
sudo chown -R $USER:$USER .
```

### 5. Запуск

```bash
# Запуск (Dockerfile автоматически соберет фронтенд)
docker-compose up -d --build
```

### 6. Проверка

```bash
# Логи
docker-compose logs -f

# Проверка работы
curl https://vildanai.ru
curl https://vildanai.ru/api
```

## Что произойдет автоматически

1. **Dockerfile соберет фронтенд** с правильным API URL (`/api`)
2. **Caddy автоматически получит SSL** сертификат через Let's Encrypt
3. **Все компоненты запустятся** в одном контейнере:
   - Caddy на портах 80/443
   - Backend API на порту 5000
   - Парсер (обновление каждые 15 минут)
   - Telegram бот (если установлен BOT_TOKEN)

## Настройка Telegram бота (опционально)

Отредактируйте `docker-compose.yml`:

```yaml
environment:
  - BOT_TOKEN=ваш_токен_telegram_бота
```

Затем перезапустите:
```bash
docker-compose restart
```

## Проверка работы

После деплоя будут доступны:

- 🌐 **Сайт**: https://vildanai.ru
- 📚 **API Документация**: https://vildanai.ru/docs
- 🔑 **Получить токен**: https://vildanai.ru/api/token
- 🔌 **API**: https://vildanai.ru/api

## Обновление

```bash
# Остановка
docker-compose down

# Обновление кода
git pull

# Пересборка и запуск
docker-compose up -d --build
```

## Troubleshooting

### SSL не получается

1. Проверьте DNS: `dig vildanai.ru`
2. Проверьте порты: `sudo ufw status`
3. Проверьте логи: `docker-compose logs caddy`

### Фронтенд не работает

1. Проверьте что фронтенд собран: `docker exec gremuiv-app ls -la /app/frontend/dist/`
2. Пересоберите: `docker-compose up -d --build`

### API не работает

1. Проверьте логи: `docker exec gremuiv-app tail -f /var/log/supervisor/gremuiv-api.out.log`
2. Проверьте здоровье: `curl http://localhost:5000/` (внутри контейнера)






