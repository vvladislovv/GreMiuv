# 📚 GreMuiv - Журнал оценок

Система для автоматического парсинга и отображения оценок студентов из Excel файлов.

## 📁 Структура проекта

```
GreMuiv/
├── parsing/              # Парсер Excel файлов
│   ├── main.py          # Главный файл парсера
│   ├── database.py      # Модели БД
│   ├── config.py        # Конфигурация
│   ├── run.py           # Запуск парсера
│   ├── parsers/         # Парсеры Excel
│   └── downloaders/     # Загрузчики файлов
├── backend/             # FastAPI бэкенд
│   ├── main.py         # API сервер
│   ├── run_backend.py  # Запуск API
│   └── start.py        # Запуск парсера + API
├── telegram/            # Telegram бот
│   ├── bot.py          # Главный файл бота
│   ├── run.py          # Запуск бота
│   ├── config.py       # Конфигурация бота
│   ├── handlers/       # Обработчики команд
│   ├── keyboards.py    # Клавиатуры
│   └── states.py       # FSM состояния
├── frontend/            # React + Vite фронтенд
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── hooks/       # Custom hooks
│   │   ├── services/   # API сервисы
│   │   └── utils/      # Утилиты
│   └── package.json
└── data/                # Данные (БД, скачанные файлы)
    ├── students.db
    └── downloaded_files/
```

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
# Python зависимости
pip3 install -r requirements.txt

# Node.js зависимости (фронтенд)
cd frontend
npm install
cd ..
```

### 2. Запустите приложение

**Единственный скрипт запуска (парсер + API + Telegram бот):**
```bash
cd backend
python3 start.py
```

⚠️ **Для работы Telegram бота:** Убедитесь, что создан файл `.env` в корне проекта с токеном бота (см. раздел "Настройка Telegram бота" ниже)

При запуске:
- Парсер автоматически выполнит первый парсинг при старте
- Данные будут обновляться каждые 15 минут по реальному времени
  (в 00, 15, 30, 45 минут каждого часа)
- Старые данные автоматически удаляются перед сохранением новых
- API сервер будет доступен на http://localhost:5000

**Фронтенд (отдельно):**
```bash
cd frontend
npm run dev
```

**Telegram бот (отдельно):**
```bash
cd telegram
python3 run.py
```

⚠️ **Важно:** Перед запуском бота необходимо:
1. Получить токен бота у [@BotFather](https://t.me/BotFather) в Telegram:
   - Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Скопируйте полученный токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. Создайте файл `.env` в корне проекта (рядом с `requirements.txt`):
   ```bash
   # В корне проекта создайте файл .env
   # Добавьте в него:
   BOT_TOKEN=ваш_токен_бота_здесь
   ```
   
   Пример содержимого файла `.env`:
   ```
   BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

## 🌐 Доступ

- **Фронтенд**: http://localhost:3000
- **API**: http://localhost:5000
- **API Docs**: http://localhost:5000/docs

## ⚙️ Настройка

Отредактируйте `parsing/config.py` для настройки:
- ID файлов Google Drive
- Интервал обновления (по умолчанию 15 минут)
- Настройки парсинга

## 📊 Функции

**Парсер и API:**
- ✅ Автоматическое обновление данных каждые 15 минут
- ✅ Автоматическое удаление старых данных перед сохранением новых
- ✅ Парсинг Excel файлов с Google Drive
- ✅ FastAPI бэкенд с автоматической документацией
- ✅ Рейтинг студентов по пропускам (по всем предметам)
- ✅ Рейтинг студентов по оценкам (средний балл по всем предметам)
- ✅ Красивый React интерфейс с таблицей оценок
- ✅ Статистика посещаемости
- ✅ Цветовая индикация оценок
- ✅ Адаптивный дизайн

**Telegram бот:**
- ✅ Многоэтапная регистрация (тег, имя, ФИО)
- ✅ Меню настроек с информацией о боте
- ✅ Удаление аккаунта из базы данных
- ✅ Просмотр оценок (в разработке)
- ✅ Mini App для журнала оценок

**Система логирования:**
- ✅ Все логи сохраняются в БД (парсер, бэкенд, Telegram)
- ✅ Детальное описание событий
- ✅ Трассировка ошибок
- ✅ Логирование действий пользователей

## 🎨 Технологии

**Parsing:**
- Python 3.7+
- SQLAlchemy
- openpyxl
- schedule

**Backend:**
- Python 3.7+
- FastAPI
- SQLAlchemy

**Frontend:**
- React 18
- Vite
- Axios

**Telegram Bot:**
- Python 3.7+
- aiogram 3.0+
- SQLAlchemy

**Логирование:**
- Все логи сохраняются в БД
- Модули: parser, backend, telegram
- Уровни: INFO, WARNING, ERROR, DEBUG
- Подробности см. в `parsing/LOGGING.md`
