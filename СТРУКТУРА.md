# 📁 Структура проекта GreMuiv

## Основные папки

### 1. `parsing/` - Парсер Excel файлов
**Что здесь:** Код для скачивания и парсинга Excel файлов с Google Drive

**Файлы:**
- `main.py` - главный файл, запускает парсер каждые 15 минут
- `database.py` - модели базы данных (Group, Student, Subject, Grade)
- `config.py` - настройки (ID файлов Google Drive, интервал обновления)
- `run.py` - скрипт для запуска парсера
- `parsers/excel_parser.py` - парсит Excel файлы
- `downloaders/google_drive.py` - скачивает файлы с Google Drive

**Как запустить:**
```bash
cd parsing
python3 run.py
```

**Зачем:** Автоматически скачивает файлы, парсит их и сохраняет данные в БД

---

### 2. `backend/` - FastAPI сервер
**Что здесь:** API сервер для получения данных из БД

**Файлы:**
- `main.py` - FastAPI приложение с эндпоинтами
- `run_backend.py` - запуск только API сервера
- `start.py` - запуск парсера + API вместе

**Эндпоинты:**
- `GET /api/groups` - список групп
- `GET /api/subjects?group_id=X` - предметы группы
- `GET /api/grades?group_id=X&subject_id=Y` - оценки
- `GET /api/stats?group_id=X&subject_id=Y` - статистика

**Как запустить:**
```bash
cd backend
python3 run_backend.py
```

**Зачем:** Предоставляет данные фронтенду через REST API

---

### 3. `frontend/` - React приложение
**Что здесь:** Веб-интерфейс для просмотра оценок

**Файлы:**
- `src/App.jsx` - главный компонент
- `src/components/` - компоненты (Select, GradesTable, StatsCard)
- `src/hooks/` - кастомные хуки для работы с API
- `src/services/api.js` - функции для запросов к API
- `src/utils/` - утилиты (цвета оценок, форматирование)

**Как запустить:**
```bash
cd frontend
npm install  # первый раз
npm run dev
```

**Зачем:** Красивый интерфейс для просмотра оценок и статистики

---

### 4. `data/` - Данные приложения
**Что здесь:** Все данные проекта

**Содержимое:**
- `students.db` - база данных SQLite (группы, студенты, оценки)
- `downloaded_files/` - скачанные Excel файлы с Google Drive

**Зачем:** Хранит все данные в одном месте

---

## Как все работает вместе

```
1. Парсер (parsing/)
   ↓
   Скачивает Excel файлы с Google Drive
   ↓
   Парсит их и извлекает данные
   ↓
   Сохраняет в БД (data/students.db)

2. Backend (backend/)
   ↓
   Читает данные из БД
   ↓
   Предоставляет через REST API

3. Frontend (frontend/)
   ↓
   Запрашивает данные через API
   ↓
   Отображает в красивом интерфейсе
```

## Быстрый запуск всего

**Терминал 1 - Парсер:**
```bash
cd parsing && python3 run.py
```

**Терминал 2 - API:**
```bash
cd backend && python3 run_backend.py
```

**Терминал 3 - Фронтенд:**
```bash
cd frontend && npm run dev
```

Или все вместе:
```bash
cd backend && python3 start.py  # запустит парсер + API
# Затем в другом терминале:
cd frontend && npm run dev
```

## Настройка

Все настройки в `parsing/config.py`:
- `FILE_IDS` - ID файлов Google Drive
- `PARSE_INTERVAL_MINUTES` - интервал обновления (15 минут)
- `DATABASE_URL` - путь к БД
